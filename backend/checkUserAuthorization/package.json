{
  "name": "check-user-authorization",
  "version": "1.0.0",
  "description": "Cloud Function to check if a user is authorized based on Firebase ID token and Datastore allow-list.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "@google-cloud/datastore": "^8.0.0",
    "firebase-admin": "^12.0.0"
  }
}
